<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.profile.collect.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/budaow"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.profile.collect.project.activity.DemoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name="com.profile.collect.project.AudioTestActivity" />

        <activity android:name="com.profile.collect.project.MainActivity" />

        <activity android:name="com.profile.collect.project.activity.GSensorActivity" />

        <activity android:name="com.profile.collect.project.activity.AnimActivity" />

        <activity android:name="com.profile.collect.project.activity.NotiActivity" />

        <activity android:name="com.profile.collect.project.activity.MediaActivity" />

    </application>

</manifest>